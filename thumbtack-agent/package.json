{
  "name": "thumbtack-agent",
  "private": true,
  "version": "1.0.0",
  "description": "Box-only Playwright agent: reads the customer email off Thumbtack's create-price-estimate page and reports it to the os API. NOT deployed to Render.",
  "scripts": {
    "start": "node src/index.js",
    "dry-run": "node src/index.js --dry-run",
    "test": "node --test src/extract.test.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "playwright": "^1.61.0"
  }
}
