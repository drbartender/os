# Build Board

Maintained by Claude as a byproduct of working, never by Dallas. It is the index: each ready item links to its spec and plan, so the thinking is one click away.

Note: this board carries titles and paths ONLY. Never paste spec or plan bodies here, and never a customer name, token, Stripe id, or any payload. Writes go through `scripts/board-write.sh`, which enforces a denylist before any commit and then commits LOCALLY. It pushes only when the board commit is the sole unpushed commit on main, and fails closed when that count cannot be determined, because `git push origin main` is a branch push that would ship everyone else's unpushed work alongside a board line. So the published board can lag the local one until the next real push; that is the design, not a failure. Keep the section headings below stable: the write helper anchors on them.

## Ready to build

<!-- plan written + reviewed, no lane cut. One line per item, linking its spec + plan. -->

- **proposal-compare-and-book** — compare and book on the proposal page. Spec only, no plan yet, so this is not lane-ready; carried here so it is not lost behind the marketing phases. Absorbs the parked compare-page reskin and Dallas's 8/10 item 8 (the client-facing compare surface is unreadable at 3 options, worse at more). NOTE 2026-08-19: section 5 of that spec (the money seam, which puts the commit point in the public sign endpoint) is SUPERSEDED by [the options-drawer design](superpowers/specs/2026-08-14-proposal-options-drawer-design.md). What is still ready here is the compare-surface readability work, not the sign-time commit. [spec](superpowers/specs/2026-08-11-proposal-compare-and-book-design.md)

## In flight

<!-- lane open and building. Stale-lane flags and stalled/orphaned branches surface here. One line per item. -->

- **consult-call-bridge** — lane 1 `consult-call-core` MERGED 2026-08-25 (`fafa0d6f`); its worktree is still on disk. **Lane 2 `consult-call-surfacing` is NOT cut** and the feature is half-built until it is: the bridge runs but nothing surfaces a failed or missed consult call in admin. Five steps S1-S5 in the plan, all unticked, five-agent fleet required. After it, a Launch checklist that is Dallas-driven ops, not a lane: Render env set BEFORE the push (`CONSULT_CALL_ENABLED=false`, `CONSULT_CALLER_ID`), then Dallas books his own slot and walks the press-1 bridge. [spec](superpowers/specs/2026-08-25-consult-call-bridge-design.md) / [plan](superpowers/plans/2026-08-25-consult-call-bridge.md)
- **beer-picks** — STALLED, not abandoned: worktree still live and CLEAN, 12 commits, tip `498723b5`, no commit since 2026-08-20. Beer catalog picks feeding the par and shopping rows, with four fleet rounds already folded in (the last two collapsed the "does this plan have beer at all" question to one owner). None of it is on main. [backlog](fix-list-remaining-2026-07-02.md)
- **branch roll-call, verified 2026-08-25** — these branches survive with no worktree and are NOT open lanes: `shift-lifecycle`, `schema-traps`, `mkt-perf`, `admin-os-legacy-palette` all shipped in the 2026-08-16 push; `mobile-asbuilt`'s docs-only commit survived the 2026-08-23 ledger restructure and is on main; `stripe-fail-closed` was REVERSED by design (main's stripeClient.js says so at the source, decided 2026-08-19) so it must never be merged; `tmp-noop` is an artifact, zero ahead. `git branch --no-merged main` lists all of them anyway, because lanes merge here as explicit-pathspec commits and a lane tip never becomes an ancestor of main. That command is not a merge check on this repo. The only genuinely unmerged orphans are the two below.
- **current-date-shift-visibility** — STALLED/ORPHANED, not building: worktree already removed, branch still alive, idle since 2026-08-14. Holds 2 unmerged commits (`18768c72`, `bd99638d`) measuring shift visibility on the Chicago business day instead of SQL `CURRENT_DATE`. None of it is on main. Only PARTIALLY superseded by shift-lifecycle: main still carries bare `CURRENT_DATE` in `server/utils/autoAssign.js`, `server/utils/autoAssignScheduler.js`, `server/utils/retentionEligibility.js`, `server/routes/clientPortal.js`, `server/routes/calcom.js` and `server/routes/admin/users.js`, which is exactly the family those commits fix. This is live unmerged correctness work: **do not scrap.** RE-VERIFIED against code 2026-08-25: all six files still carry bare `CURRENT_DATE` (46 across server/), while `chicagoTodayYmd()` already exists in `server/utils/businessTime.js` and the payroll paths already use it, so the correct pattern is established and these are simply un-migrated. Sharpest instance is `admin/users.js`, which stamps `hire_date = CURRENT_DATE`, so a contractor promoted after 19:00 Chicago gets tomorrow's hire date written to disk and hire_date feeds seniority. Any salvage now touches `server/routes/staffPortal.js`, sensitive-listed since `ec6d6028`, so the merge is a mandatory stop-and-ask. [backlog](fix-list-remaining-2026-07-02.md)
- **shift-closure** — **SCRAPPABLE, verdict corrected 2026-08-25.** This line used to say "do not scrap" and that is wrong. Main carries `server/utils/shiftClosureSweep.js`, wired hourly at `server/index.js:519`, and its header comment deliberately REJECTS this branch's design: a sweep, not a hook on the completion doors, because a proposal can carry a still-upcoming shift and the doors never fire twice, so a shift skipped once by a date gate was skipped forever. The branch's only unique piece is exactly that door-hook in `proposals/lifecycle.js`. Its `backfillShiftClosures.js` is already on main and runs. Chicago-date gating arrived separately via `shiftEndInstant.js`. Nothing is lost by deleting the branch. [backlog](fix-list-remaining-2026-07-02.md)

## Recently shipped

<!-- merged + shipped lanes, newest first. Ages off over time. One line per item. -->

- **proposal options ladder** — three lanes, oo-switch-server (`c6927359`), oo-server-display (`39ff7d8d`), oo-ladder-client (`d171662a`), all merged 2026-08-21 and all PUSHED; verified on origin/main 2026-08-25. The PUSH HOLD below was satisfied by shipping all three together. Lane branches are gone. oo-switch-server (c6927359), oo-server-display (39ff7d8d), oo-ladder-client (d171662a). Turns the bottom-of-page browse panel into a drawer where a client actually changes their own proposal pre-signature, as ONE ladder of how much DrB handles: bar service, the three BYOB tiers, then hosted. Dallas re-aimed it 8/20 after prod showed 271 of 307 proposals are bar-service-only BYOB, so the old package-switching frame served the 12% case. The hold (Dallas) was: lane 1 alone moves displayed totals on the live panel, so nothing ships until the drawer ships with it. Four fleets ran and they earned it. The client fleet found 2 criticals: the desktop panel PAINTED OVER the sticky sign-and-pay rail (the exact leak this redesign exists to fix, reached from the other side), and the whole BYOB half of the ladder rendered no commit button, so the most-asked flow was inert. One root cause: the plan required commit-flow RTL tests and I wrote chrome tests instead, so the money path shipped exercised only by the compiler. commitFlow.test.js exists now; reverting the headline fix fails 5 of its 8. A second browser pass then caught that my inset FIX was itself inert (an inline padding shorthand beat the class rule, invisible to grep and to the bundle) and that my extras-strip fix wiped the mobile ladder at the peek snap. Both fixed and re-measured. OWED: Dallas's real-device walk, since everything verified so far is headless Chromium and I called this finished twice before it was. Also open: ProposalView.js is 840 lines and wants an extraction, and the derived-not-duplicated plan (62a56f2b) carries five follow-ons these fleets surfaced outside the lanes. [spec](superpowers/specs/2026-08-20-proposal-options-ladder-design.md) / [plan](superpowers/plans/2026-08-20-proposal-options-ladder.md)

- **stale-proposal-sweep** — merged 2026-08-20 (`85c1fbcf`), PUSHED 2026-08-21 in the `9cccd3da..4ee51d00` batch: an hourly scheduler that archives past-dated proposals nobody ever booked, writing a NEW `archive_reason` of `event_passed` and reusing the manual Archive button's helpers so the two doors cannot drift. **LIVE and its backlog is drained** (rollout ran 2026-08-21, Tier 4 of walkthroughs-owed is ticked): 116 archived in ~100 seconds, 105 invoices voided, 26 pending messages deleted, zero Stripe heal markers, every safety assertion held. Steady state since: 3 more on later ticks, `scheduler_health` ok with zero failures. `accepted` is exempt and emailed to an admin instead, which is not a courtesy but the money guard — the refund demote ladder parks refunded-to-zero bookings there, so widening that status list would stamp "never booked" on a proposal someone paid. Five-agent fleet pre-merge caught one critical: the heal pass deleted its own retry queue whenever Stripe was unreachable, because a call that tried NOTHING reported the same `failed: 0` as a call where nothing failed. Three tests were passing vacuously and were rewritten mutation-verified. [spec](superpowers/specs/2026-08-20-stale-proposal-sweep-design.md) / [plan](superpowers/plans/2026-08-20-stale-proposal-sweep.md)
- **pay-period-boundary** — merged 2026-08-19 (`ec6d6028`) + `2186abe5`, NOT yet pushed: a money defect in `server/routes/staffPortal.js`, where a contractor's staff home resolved the NEXT pay period on the final evening of the current one and showed $0 for a week they had actually worked. 195 lines of boundary tests ride with the fix. **Push consequence:** the lane ADDED `server/routes/staffPortal.js` to `scripts/sensitive-paths.txt`, so the next push to main pulls the full agent fleet plus `/second-opinion`, not a light look. [plan-ref](fix-list-remaining-2026-07-02.md)
- **board-push-guard** — merged 2026-08-19 (`993d4a25`) + `61611e54`, NOT yet pushed: `scripts/board-write.sh` now skips its push unless the board commit is the ONLY unpushed commit on main, and fails closed when the count cannot be determined. `git push origin main` is a branch push, so a one-line board write was shipping every other window's unpushed work with it. Skipping is a success, not an error: the board is still written and committed, only the push defers to the next real one. [plan-ref](fix-list-remaining-2026-07-02.md)
- **mkt-moment-setup** — merged 2026-08-19 (`7b099746`), NOT yet pushed: a marketing moment whose window is open but whose audience is empty no longer renders as nothing. It was hidden in three places (card list, `open_moment_count` badge, and the header that read "0 moments open"). An open moment now has three states, and each declares `emptyAudience: 'configure' | 'wait'` so a permanently-open, time-gated moment cannot nag daily on a young book. [plan-ref](fix-list-remaining-2026-07-02.md)
- **nat-trim** — merged 2026-08-17 (`9d7d4c86`) + review fixes (`d61c62b7`), PUSHED 2026-08-19; origin/main sits at `d61c62b7`. Money tab removed from Needs-attention (Dallas reversed the earlier "leave it be"); payroll gets its own admin-only Band 1 card in the right rail, and the dormant unmatched-payout alarm keeps a mounted surface on the Band 2 Payouts button rather than being lost with the tab. Rode along: dated Staffing rows capped at a 14-day horizon. [plan-ref](fix-list-remaining-2026-07-02.md)
- **the 2026-08-16 push** — 52 commits (`bab7fba5..981b09ef`), the largest recent deploy and the one this board had recorded nothing about: ma-d-auth, shift-lifecycle, schema-traps, mkt-perf, admin-os-legacy-palette and more. Post-deploy actions and the 5-agent fleet's non-blocking findings live in the backlog's "Push 2026-08-16" section; do not re-derive them here. [plan-ref](fix-list-remaining-2026-07-02.md)
- **ma-d-auth** — WebAuthn passkey unlock for the phone admin PWA: webauthn_credentials + challenges, six routes under /api/auth/webauthn (assert-verify is the only 12h mint), lock screen in gate + overlay mounts, four-way AuthContext purge law, SW v8 /auth/me hydration, desktop passkey revoke. Merged 2026-08-14 (`c206118c`) plus the offline-session fix (`981b09ef`), PUSHED 2026-08-16 in the `bab7fba5..981b09ef` batch. Prod DDL confirmed applied: `webauthn_credentials` and `webauthn_challenges` are both live in prod, with two passkeys enrolled and in daily use. Three review rounds (mid-lane security, 4-agent fleet, codex). **Lesson, from the wrong note this entry used to carry:** "prod DDL owed before the push" was never true here, and is misleading for any DDL that lives in `server/db/schema.sql`, because `server/db/index.js` re-executes schema.sql on every boot and applies it regardless. Reserve that phrasing for backfills and data migrations, which the boot path does NOT run. [plan](superpowers/plans/2026-08-14-mobile-admin-auth.md)
- **shift-lifecycle** — merged 2026-08-14 (`22c550ea`), PUSHED 2026-08-16: the shift end-instant re-cut plus the closure sweep, over two review rounds (the second closed a FAIL). PARTIALLY supersedes the `shift-closure` and `current-date-shift-visibility` branches. Partially is the load-bearing word: both still hold unmerged work and neither is scrappable, see In flight. [spec](superpowers/specs/2026-08-14-shift-lifecycle-design.md)
- **mkt-restyle** — merged + PUSHED 2026-08-14 (`19a9298e`): the fix for the 8/14 design-never-applied incident, where the approved marketing design was vendored and cited in the spec header but never made a build requirement, so the section shipped to prod with zero visual fidelity to it. [plan](superpowers/plans/2026-08-14-marketing-restyle.md)
- **marketing-redesign phases 1+2** — LIVE IN PROD via the 2026-08-13 evening push: seven lanes (mkt-a..g) — tags, resolver, contacts UI, extract, compliance, send. Phase 3 (mkt-h overview) MERGED 2026-08-13 (`a0360039`) and was already live via the 2026-08-14 push, not the 8/16 one. Sep 5 deadline. [spec](superpowers/specs/2026-08-11-marketing-campaigns-design.md) / [plan](superpowers/plans/2026-08-11-marketing-redesign-phase-1.md)
- **bar-card** — merged 2026-08-13 (`2a40d00d`), PUSHED 2026-08-14: the shift-card chip stops calling bar-hauling shifts "Bar Kit Only" (server-derived bar_required, matching duty pay), and card headers show the time range. Companion to the bar-required ack feature that went LIVE in the 8/13 push. [plan-ref](fix-list-remaining-2026-07-02.md)
- **proposals-pagination** — merged to main 2026-08-13 (`1dc72df6`, lane `prop-pagination`, another window) and PUSHED the same evening: Prev/Next paging on the Proposals dashboard, client-only over the existing `?page`/`X-Total-Count` server support. Two follow-up steps recorded in walkthroughs-owed by that window. [spec](superpowers/specs/2026-08-12-proposals-pagination-design.md) / [plan](superpowers/plans/2026-08-12-proposals-pagination.md)
- **vm-listen-link** — merged + PUSHED 2026-08-11 in the 34-commit batch (`e0f777b7..b27b6d4e`). Token-gated route streams the retained primary recording; the alert SMS carries the URL, both sides fed by one `listenLinkEnabled()` so the kill switch cannot drift. Transcription considered and declined. Push fleet clean, codex + gemini both NO FINDINGS; residuals as fix-list 17 and 19. [spec](superpowers/specs/2026-08-10-voicemail-listen-link-design.md) / [plan](superpowers/plans/2026-08-10-voicemail-listen-link.md)
- **tip-sign-download** — both lanes (tip-a-methods, tip-b-download) merged + PUSHED 2026-08-11 in the same batch. Shared tip-method derivation extracted so the downloaded sign and the chooser page cannot disagree on normalization; print replaced by jpg/png/pdf download. [spec](superpowers/specs/2026-08-10-tip-sign-download-and-display-design.md) / [plan](superpowers/plans/2026-08-10-tip-sign-download-and-display.md)
- **fixlist-drop-8-10** — Dallas's 8/10 drop items 1, 3, 4, 5, 6 merged + PUSHED 2026-08-11 in the same batch: TT programmatic draft clear (already live on the box), staff recipes rendering real ingredients, inbound SMS alerts naming the staffer, first-name greetings, guest count in the event header. The fleet caught the greeting sweep dropping the partner for 9 couple-named clients; fixed pre-push in `dacd1619`. [plan-ref](fix-list-remaining-2026-07-02.md)
- **duty-residuals** — built, reviewed, merged + PUSHED 2026-08-07: hosted supplies to a flat $50 duty line (supply-hours model retired), out-of-area knob no-op/auto-lock semantics, ShiftDrawer knob, serviceArea geocode throttle rework, 25P02 pre-screen in extension repricing. [plan-ref](fix-list-remaining-2026-07-02.md)
- **phone-system-1a** — built + full-fleet-reviewed + merged + PUSHED 2026-08-07: two-line call experience (1922 rings Dallas, per-line greetings/voicemail, press-1 escalation ships dark). Fleet caught a signature-order DoS, an advisory-cap race, a sweep starvation blocker — all fixed + re-verified; second opinion clean. SHIP-TIME: Render env (VM_PRIMARY_DIAL_TARGET, VM_TEXT_DESTINATION) BEFORE pointing the 1922 webhooks, same sitting as the push — checklist in plan Ops section. [spec](superpowers/specs/2026-07-26-phone-system-redesign-design.md) / [plan](superpowers/plans/2026-07-26-phone-system-1a-call-experience.md)
- **owner-no-draw** — both lanes built, fleet-reviewed, merged + PUSHED 2026-08-07 (prod DDL+backfill ran BEFORE the push; push-fleet hardening c463c9e7); fleet caught a one-connection audit deadlock, an un-park finalize race, and a CI-fatal orphan; browser walks done both portals. SHIP-TIME: prod DDL + backfill (payouts 80/83/92/98, close period 72) BEFORE the push — checklist in plan. [spec](superpowers/specs/2026-08-07-owner-no-draw-payouts-design.md) / [plan](superpowers/plans/2026-08-07-owner-no-draw-payouts.md)
- **service-extension** — all 5 lanes built, merged, and PUSHED 2026-08-04 (plan rev 5.1); per-lane fleets caught two money blockers pre-merge (gratuity re-accrual on grace-window payments; stranded-alert storm throttle); push gate = integration sweep + gratuity/TT re-confirm + codex second opinion. [spec](superpowers/specs/2026-07-25-service-extension-design.md) / [plan](superpowers/plans/2026-07-26-service-extension.md)
- **event-details revival** — the three stale 7/22 staff event-details lanes revived onto fresh main as one lane, merged + PUSHED 2026-08-03; full fleet, push re-confirm, and cross-LLM second opinion (codex caught a cross-shift drop blocker, fixed pre-push). [spec](superpowers/specs/2026-07-22-staff-event-details-design.md) / [plan](superpowers/plans/2026-07-22-staff-event-details.md)
- **invoice-derivation batch** — all 5 lanes merged 2026-07-28, pushed 2026-08-03; lane 1's derivation rewrite deliberately REVERTED same evening (monitor, void UI, netting kept); redo queued provenance-first. [spec](superpowers/specs/2026-07-28-invoice-derivation-and-monitor-design.md) / [plan](superpowers/plans/2026-07-28-invoice-derivation-and-monitor.md)
- **sms-optin** — standalone /sms opt-in page (A2P fix), shipped by cherry-pick 2026-07-30 ahead of the held batch. [spec](superpowers/specs/2026-07-30-sms-opt-in-page.md)
- **onboarding-upload-and-draft** — 4 lanes (upload-honesty, onboarding-drafts, document-visibility, submit-gate-relax) merged 2026-07-26..28, pushed 2026-08-03 with fleet fixes. [plan](superpowers/plans/2026-07-26-onboarding-upload-and-draft.md)
- **addon-quantity-semantics** — merged in the 2026-07-26 window, pushed 2026-08-03. [plan](superpowers/plans/2026-07-26-addon-quantity-semantics.md)
- **email-designer** — drag-and-drop designed-email builder on the Marketing composer (design_json → server-rendered html; upload/preview/test endpoints). Merged + pushed 2026-08-03 (a0632ae3) after full fleet re-confirmation + codex/gemini second opinion; no spec doc (patch import from a claude.ai/code session).
- **gratuity-election-at-payment** — Delara 665 root cause: checkout tip-jar election persisted before payment, baking "mandated" gratuity into abandoned quotes. 3 lanes (grat-intent-webhook `b6b1910c`, grat-admin-lockdown `d72461eb`, grat-copy-reset `85492d36`) merged AND PUSHED 2026-08-04; election now rides intent metadata and persists only on payment success, admin preset removed. The prod reset that was gated on deploy is closed by OUTCOME, not by a recorded run: re-checked against prod 2026-08-19, zero unpaid proposals carry a client-elected gratuity (`amount_paid = 0 AND gratuity_rate > 0 AND gratuity_floor_rate IS NULL` returns no rows), which is the invariant the reset existed to restore. Whether those rows were reset by hand or simply aged out is not recorded anywhere, so do not claim the reset was executed. [spec](superpowers/specs/2026-08-03-gratuity-election-at-payment-design.md) / [plan](superpowers/plans/2026-08-03-gratuity-election-at-payment.md)
- **staff-display-name + seniority-backfill + gate-fixes** — the 8/05 batch PUSHED 2026-08-06 (677baf95): display-name rollout, seniority baseline + CC import tooling, and the push-gate fix lane (SMS-tiebreak trigger fix, zeroing fix, omitted-name keep). Prod DDL + display-name backfill ran BEFORE the push (zero-outage order); codex/gemini second opinion clean. [spec](superpowers/specs/2026-07-26-staff-display-name-design.md) / [plan](superpowers/plans/2026-07-26-staff-display-name.md)
- **vm-press1-honesty** — merged 2026-08-20 (`5016dc2c`), NOT pushed: the press-1 Gather now follows the GREETING instead of VM_ESCALATION_ENABLED, so flipping the kill switch during an incident can no longer invite every daytime caller to press a key nobody is listening for. No-op in the current prod configuration (the switch is on), and the switch keeps its real guarantee: voiceEscalate.js tests it itself, so off is still zero claims and zero billed legs. Two corrections to the recorded finding came out of verifying it: the trap was on BOTH lines, and its prescribed revert did not work because the synthetic greeting text says the offer too. **Push consequence:** voice.js and voicemailTwiml.js are both sensitive-listed, so this pulls the full fleet plus /second-opinion at push. [plan-ref](fix-list-remaining-2026-07-02.md)
- **pushgate-814-residuals** — merged 2026-08-20 (`5117ef3e`), NOT pushed: three real findings out of the 2026-08-14 push gate's eight non-blocking notes. An ARCHIVED campaign could win the marketing send claim and mail, because the claim's WHERE only asked about 'sending' and the archive writer happily archives a draft in the gap after the route's read. Decomposed accents and iOS curly apostrophes were rejected by a letters-only message that names apostrophes as allowed, in both the server validator and its client mirror. The client display-name port never followed the server's astral-plane hardening, and the server's own hardening turned out to have no test at all. Every fix mutation-verified; the okina is deliberately left unfolded. Light-look lane: sensitive-match reports none of the three files listed. [plan-ref](fix-list-remaining-2026-07-02.md)
- **cant-overnight-ordering** — merged 2026-08-20 (`74aa4a5d`), NOT pushed: on the inbound-SMS CANT write path, a past-dated shift can no longer outrank a current one. A NULL end_time takes the assumed-end rule, so a 9 PM start stays a candidate until about 5 AM, and a bartender who finished at 1 AM and texted CANT at 2 AM meaning a later date had last night's shift denied and re-opened, off the roster payroll accrues from. The new ORDER BY term is a tiebreak, not a filter, so nothing becomes invisible. **Push consequence:** the lane ADDED server/utils/smsInbound.js to sensitive-paths.txt (its thin webhook was listed and the brain was not), so this pulls the full fleet plus /second-opinion at push. [plan-ref](fix-list-remaining-2026-07-02.md)
- **dead-suites-dotenv** — merged 2026-08-20 (`41081fea`), NOT pushed: nine test suites had never run. Each was missing the dotenv line, so DATABASE_URL was unset, pg fell back to a local socket, and every DB-touching test died on ECONNREFUSED before asserting anything. Eight of the nine are the service-extension money feature and its curfew engine, which shipped 2026-08-04 with per-lane fleets; its own suite has produced zero signal since the day it was written. The ninth is the SMS consent log. 113 assertions revived, all green, and no bug behind them: they were covering nothing, not covering something broken. Sweep closed rather than sampled, all 359 server test files partitioned and the other 80 each run. [plan-ref](fix-list-remaining-2026-07-02.md)
- **constraint-contract-widen** — merged 2026-08-20 (`49d6f919`), NOT pushed: the boot-time CHECK-constraint guard now holds eight constraints instead of five, including users_role_check, the AUTH one, whose schema.sql site is a bare drop-then-add on a double definition, so a failed ADD commits a users.role column that accepts any string. Values verified against prod as well as dev before contracting them, since a wrong assertion starts a boot alert on every deploy. The catalog lookup is keyed on table.constraint now, because constraint names are unique per table and the bare-name map could answer about the wrong one. The recorded substring-match defect was RETRACTED after testing it: requiring both quotes already prevents that shape, and the real hole is a multi-arm check, which is latent and left open rather than half-fixed. [plan-ref](fix-list-remaining-2026-07-02.md)
- **agreement-bold-runs** — merged 2026-08-20 (`40607383`), NOT pushed: the contractor agreement's signing screen printed literal asterisks around clause 6's bold lead-ins, while the archived PDF has always rendered them properly. The person signing saw a worse document than the one that gets filed. Fixed in the renderer rather than the frozen copy, because V3 aliases most of V2's clause objects and editing the strings would rewrite already-signed v2 agreements. Two tests across the seam, since CRA cannot reach the real clause text: one pins the split, one pins that the frozen copy only contains runs the split understands. [plan-ref](fix-list-remaining-2026-07-02.md)
- **positions-needed-one-reader** — merged 2026-08-20 (`dac9c9f0`), NOT pushed: the admin dashboard had its own bare-JSON.parse reader of positions_needed, which server/utils/positionsNeeded.js explicitly forbids because the column holds two historical shapes. On the legacy object shape the dashboard counted a two-bartender shift as needing one, so it left every unstaffed queue as soon as one person was approved and the second bartender was never hired. remainingByRole in the same file already used the correct reader. Latent on prod (all 72 shifts carry flat canonical values, checked), and the recorded fix would have made it worse: dropping the empty-roster fallback to 0 reads as fully staffed across four surfaces. [plan-ref](fix-list-remaining-2026-07-02.md)
- **dead-affordances** — merged 2026-08-20 (`b7c8119b`), NOT pushed: three surfaces stop looking tappable when they are not. The staff pending shift card sits as an adjacent sibling to live openable cards in the same flex column and carried a pointer cursor and hover border in both skins; the Field Guide's sections could not be opened by keyboard at all, while the attestation below them could be checked and signed, so you could sign what you could not read; and every line of the client shopping list was a bare div with a hand-drawn checkbox, a total keyboard lockout on the public page a client works an aisle with. Took the subtractive option on the pending card deliberately: whether a pending staffer may read the event brief is a product call. Three new test suites where these surfaces had none, all mutation-verified. [plan-ref](fix-list-remaining-2026-07-02.md)
- **review-findings-fix** — merged 2026-08-20 (`37baf006`), NOT pushed: the fleet the two sensitive lanes were owed, plus every fix it produced. Both external reviewers returned no findings; the Claude fleet found eight, three of them mine. The sharpest: the .env.example correction written earlier the same day was itself a false prescription (setting the env var short-circuits the very flag the recipe told you to flip), and my mutation verification had picked a mutant that failed while a weaker one passed 75/75, so the migration path was never pinned at the route. Both closed with tests. Left open for Dallas and recorded: the staff-portal next-shift card still orders differently from the CANT write path, verified unreachable on prod today; and smsConsent.js plus proposals/public.js write the SMS opt-out field while unlisted. [plan-ref](fix-list-remaining-2026-07-02.md)
- **review-dismiss-lock** — merged 2026-08-21 (`00acdf8f`), NOT pushed: the staff-hub Dismiss button now says why it is locked before you click it, closing the half of spec section 7 that was promised and never built. The list payload carries a per-review bounty_lock derived from the same predicate the server refusal uses, so the button and the 409 cannot tell different stories. Folded in the sibling copy bug: the refusal said 'already paid' for all three frozen states, so an admin whose pay run was merely processing was sent after money that had not moved. ResolvedTable.js had no test file before this. [plan-ref](fix-list-remaining-2026-07-02.md)
**events-staff-hover (merged 2026-08-25, 21ec7993)**: hover the Staffing column on /events to see who is confirmed, name and position. Server adds an approved_staff aggregate to the admin shifts feed, filtered exactly like approved_count so the names always add up to the ratio beside them. Client adds StaffHoverCard, portaled to body because the table wrapper clips anything positioned in a cell, inert so it never steals the row's click, and flipped above the cell when there is no room below. The browser checkpoint earned its place twice: it caught the card landing 42px under the fold on the last row of a long list, and the pre-merge fleet caught the card printing a raw lowercase position where the drawer printed it canonically. Both fixed and pinned by tests. Fleet clean pre-merge and re-confirmed against main's new HEAD. OWED: Dallas's walk (walkthroughs-owed Tier 6, moves to 3b on push). [plan](superpowers/plans/2026-08-25-events-staff-hover.md)
refund-override-sync — a contract-scope refund now lowers total_price_override with total_price, so a later editor save stops re-billing the refunded amount as an Additional Services invoice (7459e1a2). Backfill still owed on proposals 599 and 527.
**proposal-pkg-fold** — merged 2026-08-25 (0f1a88f2), NOT pushed: the proposal editor's package list stopped rendering all 16 active packages as full-width description cards. Hosted packages and classes each got a fold that starts shut on every mount, measured at 5249px -> 3622px of editor on proposal 9818; the loose remainder (today just the one service-only package) stays flat. A shut fold names the selected package in its header, so the screen still tells you what the proposal is on without opening anything. partitionPackages is exported so the hosted predicate stays the same test the money code runs, and classes key on bar_type, never category, because the live class rows carry category byob despite the schema comment saying otherwise. Light-look lane: sensitive-match reports neither file listed. Card markup moved verbatim, so folded and loose cards are pixel-identical to the old list. OWED: nothing; verified headless with a real dev session on both editor mounts. [plan-ref](fix-list-remaining-2026-07-02.md)
- **ct-day-sweep** — merged 2026-08-25 (`008f4dda`), NOT yet pushed: thirteen admin reads of a TIMESTAMPTZ derived their calendar day by slicing the ISO string, which is the UTC day, so anything at or after 19:00 Chicago (18:00 in CST) displayed the day AFTER. Follows `797f418c`, which fixed the proposals Sent and Last viewed columns where the same bug was visible as the literal word "Tomorrow" (77 of 313 prod rows wrong in Sent, 63 of 279 in Last viewed). `client/src/components/adminos/format.js` now carries `ctDay` and `relDayTs`, and formatted dates compose as `fmtDate(ctDay(ts))`. The sharp edge is what was left ALONE: `blog_posts.published_at` and `stripe_payout_lines.available_on` are dates wearing a TIMESTAMPTZ, stored at UTC midnight (204 of 210 rows for available_on), so sweeping them would show the day BEFORE the one meant. The test is the data spread, not the column type, and format.js now says so. [plan-ref](fix-list-remaining-2026-07-02.md)
**no-manual-events** — manual event creation removed (POST /shifts, the Events dashboard create form, the legacy staffing form). Events now exist only via a paid proposal. e05f8c37
**consult-call-bridge (core lane)** — merged 2026-08-25 (`fafa0d6f`), NOT yet pushed and ships DARK: `CONSULT_CALL_ENABLED=false` must be set in Render BEFORE the push, the switch defaults on. Auto-rings Dallas at each Cal.com consult slot (T-90s, +60s, +180s, 20s each), hops to Zul on a third miss, press 1 bridges to the booker's own typed number showing the 1922. 21 commits, 12 review passes plus a whole-branch review that returned READY TO MERGE. Surfacing lane (attention feed, proposal and client detail lines, settings label) is spec'd and NOT cut. Owed: the launch call in walkthroughs-owed Tier 6, gated by the Tier 4 env step. [spec](superpowers/specs/2026-08-25-consult-call-bridge-design.md) / [plan](superpowers/plans/2026-08-25-consult-call-bridge.md)
