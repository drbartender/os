node_modules/
.env
.env.*
!.env.example
*.key
*.pem
client/node_modules/
client/build/
server/uploads/*
!server/uploads/.gitkeep
*.log
.DS_Store

# Claude Code / agent runtime artifacts
.playwright-mcp/
.claude/scheduled_tasks.lock
client/.claude/

# UI / pre-deploy review run output
.review-screenshots/

# Loose screenshots at repo root (intentional assets live under client/public/ or client/src/)
/*.png

# Lab Rat runtime data — mission completions are still filesystem-based
# (tester bugs moved to the `tester_bugs` Postgres table 2026-05-10).
server/data/mission-completions.jsonl
