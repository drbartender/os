# money-smoke-list.txt — money-path suites run by the pre-push gate
# (scripts/testdb-smoke.js). One repo-relative test file per line; blank lines
# and #-comments ignored. Every listed file MUST exist — a missing file is a
# HARD FAIL, not a skip (a silently-shrinking gate is worse than none).
#
# Assembled from EXISTING money-path suites only. Slow, rate-limiter-bound
# suites (e.g. proposals/crud.test.js) are deliberately excluded — the full
# suite is stage 2's job. Keep this list ~2.5 min serial or trim.

# Payroll accrual / clawback / late-tip
server/utils/payrollAccrual.test.js
server/utils/payrollClawback.test.js
server/utils/payrollLateTip.test.js
server/utils/payrollMath.test.js

# Contractor duty pay (spec 2026-08-06): trigger/reconcile engine, accrual
# derivation incl. the M5 analog + payout-stub guard, single-writer totals
# (whole-sum clamp), admin duty API + Process gate. ci-smoke runs initDb first,
# so the new tables exist there and carry prod CHECK constraints.
server/utils/dutyLines.test.js
server/utils/payrollAccrual.duty.test.js
server/utils/payrollProcessing.test.js
server/routes/admin/payrollDuty.test.js

# Owner no-draw (spec 2026-08-07): the toggle route's guard set (takes_draw
# authority, confirm_finalize one-way door, mark-paid 409 on no_draw) and the
# staff blended-total/paystub boundary live at the ROUTE layer, not the engine.
server/routes/admin/payroll.test.js
server/routes/staffPortal/payouts.test.js
# The staff-home pay-period boundary (2026-08-19). Same reason as the line above:
# it decides WHICH period a contractor is shown and what they are owed in it, and
# it shipped a defect that emptied that card on the last evening of every period.
server/routes/staffPortal.payPeriod.boundary.test.js
# The live-vs-test Stripe decision point. stripeClient.js is sensitive-listed and
# fails closed; this is its first and only suite (added 2026-08-19).
server/utils/stripeClient.test.js

# Autopay durable-charge trio
server/utils/autopayDurableCharge.test.js
server/utils/balanceScheduler.autopayDurable.test.js
server/routes/stripe.chargeBalanceDurable.test.js

# Stripe webhook guards + last-minute checkout completion
server/routes/stripeWebhook.guards.test.js
server/routes/stripeWebhookHandlers/checkoutSessionCompleted.lastMinute.test.js

# Checkout gratuity election-at-payment (spec 2026-08-03): create-intent charges
# the in-memory gratuity-inclusive total and stamps the election into intent
# metadata; the webhook applies it before the credit + Balance invoice.
# pricingEngine is pure (no DB) but guards the no-jar floor on the DERIVED rate —
# the invariant whose absence let a sub-floor rate be charged and then strand the
# payment behind an unwritable DB CHECK (fix round 1). Cheap; keep it gated.
server/utils/pricingEngine.test.js
server/routes/stripeCreateIntent.test.js
server/routes/stripeWebhook.gratuityApply.test.js

# Refunds + invoice lifecycle
server/utils/balanceInvoiceMonitor.test.js
server/routes/invoices.refunds.test.js
server/utils/refundHelpers.test.js
server/utils/invoiceLifecycle.additionalInvoice.test.js
server/utils/invoiceLifecycle.external.test.js
server/utils/invoiceLifecycle.voidRemint.test.js

# Cancel line item (fold legs + scoped refunds + routes)
server/utils/refundHelpers.scope.test.js
server/utils/lineItemCancel.test.js
server/routes/proposals/cancelLineItem.test.js
server/utils/proposalExtrasFold.stability.test.js
server/utils/addonQuantity.test.js

# Service extension (ext-core: pricing delta, settle core, payroll hours, end-instant)
server/utils/serviceExtensionPricing.test.js
server/utils/serviceExtensionSettle.test.js
server/utils/serviceExtensionPayroll.test.js
server/utils/eventEndInstant.test.js
server/utils/payrollAccrual.extension.test.js
server/routes/stripeWebhookHandlers/paymentIntentSucceeded.extension.test.js
server/utils/serviceExtensionSweep.test.js
server/utils/refundHelpers.extensionScope.test.js

# Out-of-Area Bonus (spec 2026-08-06 §6): lock lifecycle on every roster path,
# auto-lock, duty-line-follows-the-lock; remote-staffing send-check gates.
server/routes/shifts.bonus.test.js
server/routes/proposals/remoteStaffing.test.js

# Review money (spec 2026-08-06 §7): settleBounty single door, contest award.
server/routes/admin/staffReviews.test.js

# 2:00 AM insurance curfew (liquor liability warranty PK810225): proposal-side
# gates + extension-side curfew clamp (eventEndInstant + serviceExtensionPricing).
server/utils/serviceCurfew.test.js
server/utils/eventEndInstant.curfew.test.js

# 1099 tax surfaces (display-name batch 677baf95, added to the gate 2026-08-14):
# /payroll/tax-totals must label a contractor by their LEGAL name (agreements
# .full_name), never the portal nickname, and the contractor payment-history
# blended total must count REAL money only. The second test drives the actual
# paidPayoutCents path through the route and pins the owner no-draw exclusion
# (spec 2026-08-07) at the TAX surface, which payroll.test.js/payouts.test.js
# cover only at the payout surface. A wrong 1099 is a filed government document.
server/routes/admin/payrollTax.legalName.test.js

# Tip money reads (staff hub, 2026-08-19). sh-a-server put
# server/routes/admin/contractorTipPage.js on sensitive-paths.txt and created
# its first-ever suite, but left the suite off this gate: the same
# listed-but-ungated shape this file documents elsewhere. The suite pins the
# adminOnly guard on /tips and /tip-feedback (they expose per-bartender tip
# money and guest PII), the Status projection the cross-staff ledger derives
# every row from, and the target_user_id filter the staffer profile reads.
server/routes/admin/contractorTipPage.test.js

# Stripe payout mirror (2026-08-20). server/utils/stripePayoutSync.js is on
# sensitive-paths.txt but neither of its suites was on this gate: listed but
# ungated, the same shape the contractorTipPage entry above was added to close.
# Lane payout-stripe-guard made the Stripe client fail CLOSED here (a typed
# ExternalServiceError instead of a silent undefined deref), and the
# unconfigured suite is what pins that guard. The mirror is read-side and never
# moves money, but a regression here strands payout rows and blinds the
# reconciliation the payout tracking depends on.
server/utils/stripePayoutSync.unconfigured.test.js
server/utils/stripePayoutSync.test.js
