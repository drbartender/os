# Sensitive paths: single source of truth (workflow-redesign spec, 2026-06-19).
#
# A change touching ANY path here is money / auth / data / external-send risk.
# This ONE list is the trigger for all three of:
#   1. full agent-fleet review (either track), no matter how small the change
#   2. conflict-escalation (stop and ask on a conflict in these files)
#   3. auto-pull disqualification (these wait for an explicit go)
#
# Format: gitignore-style, repo-root-relative, one pattern per line.
# '*' matches within a single path segment (it does NOT cross '/').
# Blank lines and lines starting with '#' are ignored.
# Consumed by scripts/sensitive-match.js.

# Pricing / payments / PII
server/utils/pricingEngine.js
server/utils/stripeClient.js
server/utils/encryption.js
server/utils/gratuityLabels.js
server/utils/eventTypes.js
server/utils/payroll*.js

# Staffing money seam: shift_requests.position resolution feeds the payroll tip
# split (LOWER(position)='bartender'). shifts.approval.js resolves + writes it;
# the cover/drop marketplace (coverBroadcast + staffShiftActions) matches and
# writes canonical position on a claim.
server/routes/shifts.approval.js
server/utils/coverBroadcast.js
server/routes/staffShiftActions.js

# Schema / DDL (no migrations/ dir exists; schema goes through schema.sql
# plus the one-off repair scripts under server/scripts/)
server/db/schema.sql
server/scripts/*.sql

# Webhook + inbound handlers (flat in server/routes/, not a folder)
server/routes/stripeWebhook.js
server/routes/stripe.js
server/routes/calcom.js
server/routes/emailMarketingWebhook.js
server/routes/sms.js
server/routes/thumbtack.js
# Public SMS opt-in form (2026-07-30). Unauthenticated POST that INSERTs a
# clients row from attacker-controlled input, writes sms_consent_log (the
# compliance evidence we would hand a carrier in a dispute), and sets
# communication_preferences.sms_enabled — the SOLE enforcement of an SMS opt-out
# on the send path. Its sibling routes/sms.js is listed above, and this file was
# split OUT of it precisely to keep the public write small; listing it is what
# stops that split from hiding a public write from review-scaling. Same
# "sensitivity follows the code" rule as the drinkPlans and stripeWebhook splits
# below. Its own review found a portal-takeover primitive here, so a light look
# on the next edit is not acceptable.
server/routes/smsOptIn.js

# Billed-voice surfaces (2026-07-18 push review): outbound Twilio calls cost
# real money and the webhook routes take attacker-reachable input. Includes
# the pre-existing Zul-bridge gap (voice.js/telegram.js/utils/sms.js holds
# placeBridgedCall) alongside the new lead-call bridge.
server/routes/voiceLeadCall.js
server/routes/voice.js
server/routes/telegram.js
server/utils/leadCallTrigger.js
server/utils/twilioSignature.js
server/utils/sms.js
# 224-inbound voicemail (2026-07-22): telegram.js carries the audio upload and
# every Bot API send; voicemail.js constructs the authenticated Twilio media
# URLs and issues the destructive recording delete. Neither matched an existing
# glob (server/utils/* globs never cross '/'), so both were invisible to
# review-scaling until listed explicitly.
server/utils/telegram.js
server/utils/voicemail.js
# pendingCall.js owns the VA-calling claim-then-call primitives, the DB-backed
# outbound spend-cap count, and the voicemail ledger prune. Matched no glob for
# the same reason as the two above.
server/utils/pendingCall.js

# Auth / guards / limits / the shared error funnel
server/middleware/auth.js
server/middleware/rateLimiters.js
server/middleware/asyncHandler.js
server/utils/errors.js

# Schedulers (scattered in server/utils/, not a folder)
server/utils/*Scheduler.js

# Comms / external sends (flat in server/utils/, not a comms/ folder)
server/utils/*EmailTemplates.js
server/utils/*Handlers.js

# Env-var contract
.env.example

# Route-body extractions from shifts.js (2026-07-14 split): cancel-or-unassign
# denies approved shift_requests + BEO-nudge suppression — same class as its
# listed siblings shifts.approval.js / staffShiftActions.js.
server/routes/shifts.handlers.js

# Glob gap found 2026-07-14: the patterns above miss the BASE template/dispatcher
# files (emailTemplates.js does not match *EmailTemplates.js; the dispatcher does
# not end in Scheduler.js). Explicit entries:
server/utils/emailTemplates.js
server/utils/scheduledMessageDispatcher.js
server/utils/dispatcherDeadLetters.js

# Sole autopay double-charge guard; matched no existing glob (known-bugs B12, 2026-07-14).
server/utils/autopayDurableCharge.js

# Public, token-gated handler that WRITES total_price + total_price_override and
# drives the invoice lifecycle off client-supplied selections. It re-priced Jack
# Van Dyke's CC contract at catalog and overbilled him $627 (2026-07-16) while
# matching no glob on this list, so the change that broke him scaled to a light
# look. A client-triggered write to a money column belongs here.
server/routes/drinkPlans/submit.js

# Comms registry: drives every admin-initiated external send (email/SMS to
# clients) plus the invoice draft->sent flip, and renders money copy
# (invoiceSend amount due). Matched no glob above (server/utils/*Handlers.js
# does not cross '/'); found at the 2026-07-18 push review, where invoiceSend
# rendering gross amount_due instead of the remaining balance had scaled to a
# light look. Same class as the 2026-07-14 explicit-entry gaps.
server/utils/comms/*.js
server/utils/comms/actions/*.js
server/routes/comms.js

# Notify-client contract (2026-07-22): the proposal PATCH + record-payment now
# carry the client-send opt-in (notify list / notify_client) alongside their
# existing money math, and rescheduleProposal owns re-anchoring + the
# balance-due recompute + the reviewed-text send. All three were previously
# unlisted (the spec's review posture assumed otherwise; verified false at the
# rev-2 verification), so the money paths carrying most of this project scaled
# to a light look. Explicit entries:
server/routes/proposals/crud.js
server/routes/proposals/actions.js
server/utils/rescheduleProposal.js

# Same contract, the halves the entries above missed (push review, 2026-07-22).
# The refund side moves money AND emails the client, and the two shared
# primitives are what every one of the four trigger sites runs through, so a
# change to either silently re-aims all four. cancel.js additionally issues
# Stripe refunds directly.
server/routes/proposals/cancel.js
server/utils/refundClientNotify.js
server/routes/proposals/notifyPreflight.js
server/utils/clientNotices.js

# drinkPlans split fallout (push review, 2026-07-22). submit.js is listed above
# and is a token-gated PUBLIC route, but the fs-split lane moved its untrusted-
# input sanitizing and its client-send out into these siblings. Sensitivity
# follows the code, not the filename it used to live in.
server/routes/drinkPlans/submitSanitize.js
server/routes/drinkPlans/submitNotify.js

# Onboarding promotion (found 2026-07-26 while reviewing the promotion-gate
# lane). payment.js is where a staff account is granted
# onboarding_status='approved' — the status requireOnboarded (shifts.js:32)
# gates every staff-data surface on. A public register + one POST here was a
# live self-promotion hole for months, and the change that closed it matched
# NO glob on this list, so an auth-perimeter fix scaled to a light look. The
# promotion rule now lives in the helper, so both belong here.
server/routes/payment.js
server/utils/onboardingPromotion.js

# THE MONEY CORE ITSELF (push review, 2026-07-26). Found while computing the
# trigger for the cancel-line batch: the list gated the ROUTES that move money
# and the notice that announces it, but not the reconciliation code underneath.
# Same "sensitivity follows the code" rule as the drinkPlans split above: when
# stripeWebhook.js was split into per-event handlers, the money logic left the
# listed file. Every path below writes proposals.total_price / amount_paid,
# invoice amount_due / amount_paid, or issues a Stripe refund.
#
# Webhook handlers: chargeRefunded is the refund backstop (and the tip
# clawback), paymentIntentSucceeded is the amount_paid roll-up, payout mirrors
# Stripe payouts, disputes move money out. stripeWebhook.js (listed above) is
# now only the signature check + dispatch.
server/routes/stripeWebhookHandlers/*.js
# Refund core: planRefund + applyRefundReconciliation decide whether a refund
# lowers total_price; refundExecute is the ONLY caller of stripe.refunds.create.
# (Glob supersedes the refundClientNotify.js entry above; refundSweepScheduler
# was already covered by *Scheduler.js.)
server/utils/refund*.js
# Invoice money: amount_due / amount_paid / line items / payment linking /
# void + reconcile. refreshUnlockedInvoices alone rebuilds what a client owes.
server/utils/invoice*.js
# Proposal money primitives: the contract-safe reprice (override moves by the
# catalog delta), the demote-only status ladder + autopay disarm, and the
# contract-vs-extra label constant every refund classifies against.
server/utils/proposalExtrasFold.js
server/utils/proposalStatus.js
server/utils/proposalMoneyShared.js
# Cancel line item (shipped 2026-07-24): removes a priced line AND issues the
# overpayment refund in one act; cancellationMath is the cancel-event refund.
server/utils/lineItemCancel.js
server/routes/proposals/cancelLineItem.js
server/utils/cancellationMath.js
# Same contract as refundClientNotify above (the money side emails someone):
# the cancel-line notices announce a money change to a client and carry the
# MANDATORY staff tip-jar notice. Missed in the first pass of this same block
# (consistency review, 2026-07-26).
server/utils/gratuityStaffNotice.js
server/utils/lineItemRemovedNotify.js
# Payout mirror. stripeWebhookHandlers/payout.js is a thin dispatcher; the
# substance is here — writes money-bearing cents columns and ATTRIBUTES lines
# back to payments / tips / refunds. Sensitivity follows the code (chunk-3
# review, 2026-07-26).
server/utils/stripePayoutSync.js
server/routes/stripePayouts.js

# Upload perimeter (2026-07-26, onboarding-upload plan). fileValidation.js is the
# magic-bytes allowlist every upload route trusts, and this batch widens it to admit
# OLE and zip containers (DOC/DOCX) for onboarding documents. application.js and
# contractor.js are the two routes that call the widened validator, and application.js
# additionally owns the submit-time file gate that a later lane makes conditional on
# pre_hired. Removing a server-side validation gate is the same class as the
# payment.js self-promotion hole listed above: it matched no glob here, so it would
# have scaled to a light look at push.
server/utils/fileValidation.js
server/routes/application.js
server/routes/contractor.js
# Global upload middleware: the size limit and its 413/abort behavior apply to EVERY
# upload route in the app, so a change here has app-wide blast radius.
server/index.js
